% Diversified Biotech SPOT-2000
% The maximum square which fully fits inside the 0.5" (13mm) diameter circular
% stickers is 0.35" (9mm) wide.

\documentclass[letterpaper]{article}

[% INCLUDE style.tt %]

\usepackage[newdimens]{labels}
\LabelCols=12               % Number of columns of labels per page
\LabelRows=16               % Number of rows of labels per page

% Measured margins to edges of circles, plus 2mm to inset from circle diameter
% to inner square side.  The label page is slightly slimmer than letter, so the
% right margin is is increased by 1/16" or 1.6mm.
\LeftPageMargin=14mm        % These four parameters give the
\RightPageMargin=16.6mm     % page gutter sizes. The outer edges of
\TopPageMargin=15mm         % the outer labels are the specified
\BottomPageMargin=16mm      % distances from the edge of the paper.

% 3mm gap between labels + 4mm inset from label edge (13mm diameter - 9mm square side)
\InterLabelColumn=7mm       % Gap between columns of labels
\InterLabelRow=7mm          % Gap between rows of labels

\LeftLabelBorder=0mm        % These four parameters give the extra
\RightLabelBorder=0mm       % space used around the text on each
\TopLabelBorder=0mm         % actual label.
\BottomLabelBorder=0mm      %

\LabelInfotrue

[% INCLUDE labels.tt %]
